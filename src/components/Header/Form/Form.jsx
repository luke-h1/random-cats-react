import React, { useEffect, useState } from 'react';

const Form = () => {
  const [cats, setCats] = useState('');
  const fetchData = async () => {
    const API_URL = ``
  };

  useEffect(() => {
    fetchData();
  });

  return (
    <>

    </>
  );
};
export default Form;
